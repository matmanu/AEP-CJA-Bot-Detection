# Installs everything (equivalent to `pip install -e ".[all]"`). For a
# lighter install — e.g. just the standalone detection engine, no AEP — use
# the extras defined in pyproject.toml instead:
#   pip install -e ".[detectors]"        # detection engine only
#   pip install -e ".[detectors,aep]"    # + the AEP/SFTP pipeline
#   pip install -e ".[all]"              # everything, including notebooks and tests

# --- Core (src/bot_detection/core.py) ---
pandas>=2.0

# --- AEP pipeline (src/bot_detection/aep_pipeline.py, cli.py) ---
psycopg2-binary>=2.9
paramiko>=3.4
python-dotenv>=1.0

# --- Detection ensemble ---
# Optional: the script degrades to the reinforcement regex alone if these are
# missing, but coverage drops noticeably without at least one of them.
device_detector>=5.0        # Python port of Matomo's device-detector
crawler-user-agents>=0.19   # pattern list maintained by monperrus
crawlerdetect>=0.1          # port of the CrawlerDetect PHP library
regex>=2023.0                 # used internally by device_detector, if present
PyYAML>=6.0                   # device_detector's regex definition files

# --- Notebooks ---
tqdm>=4.65
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
pyod>=1.1
xgboost>=2.0

# --- Tests ---
pytest>=7.0
